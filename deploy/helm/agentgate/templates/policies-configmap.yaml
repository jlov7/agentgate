apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agentgate.fullname" . }}-policies
  labels:
    {{- include "agentgate.labels" . | nindent 4 }}
data:
  default.rego: |
{{ .Values.policy.rego | indent 4 }}
